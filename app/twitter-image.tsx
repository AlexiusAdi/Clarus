/**
 * X renders the same card as everywhere else — no separate design, just the
 * separate route name the crawler looks for.
 */
export { default, alt, size, contentType } from "./opengraph-image";
